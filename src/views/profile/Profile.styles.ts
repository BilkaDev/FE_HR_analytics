import { Styles } from '../../theme/theme';

export const container: Styles = {
  display: 'flex',
  flexDirection: 'column',
  rowGap: 1
};

export const languageContainer: Styles = {
  display: 'flex',
  alignItems: 'center',
  gap: 0.5
};
